package org.firstinspires.ftc.teamcode.Code_Under_Development.hardware.Odometry.Pathing.Follower;

import org.firstinspires.ftc.teamcode.Code_Under_Development.hardware.Odometry.ObjectAvoidance.Vector2D;

import java.util.ArrayList;

import javax.annotation.Nullable;

public class FollowerSudoCode {

    // find current target point on the path based on a hardcoded value

    // calculate pathing power, we could do this in a few ways
    //    1:have a method that uses the max velocity of the x and y direction to be able to
    //         take a movement direction in degrees and out putting the pathing powers for each motor

    // find any error in the path and calculate the corrective power

    // find turning error and calculate the pivot power

    // apply the powers to the motors in this order of importance
    // 1 -- corrective power
    // 2 -- rotation power
    // 3 -- pathing power
    
}
